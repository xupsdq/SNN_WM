experiment_name: ux_shuffle_memory_collapse
output_dir: results/ux_shuffle_memory_collapse
seed: 42

data:
  dataset_name: mnist
  dataset_root: MNIST

model:
  name: sdnn_deep_final
  path: results/sdnn_deep_final/net_final.pth

runtime:
  device: auto

plotting:
  output_dir: results/ux_shuffle_memory_collapse/figures
  dpi: 300
  formats: [png, pdf, svg]
  style_name: paper_default

